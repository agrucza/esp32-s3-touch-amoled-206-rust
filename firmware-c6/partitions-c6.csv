# ESP-IDF partition table for the ESP32-C6 build of this firmware.
#
# Selected at flash time by the espflash runner in
# `.cargo/config.toml`.
#
# Keep the `storage` row in sync with `FLASH_FS_START` /
# `FLASH_FS_SIZE` in `firmware-s3/src/system/flash_fs.rs` (under the
# board-c6 cfg branch) when persistent storage gets wired up on C6 -
# firmware doesn't read the partition table at runtime, so drift
# between the CSV and those constants lands writes in the wrong region.
#
# Layout targets a 16 MB flash chip. Proportions mirror the S3
# layout (25% firmware partition, ~75% storage, 64 KB coredump tail):
#
#   0x00_0000  bootloader + partition table   (< 0x9000, managed by espflash)
#   0x00_9000  nvs                            (24 KB, reserved for ESP-IDF NVS)
#   0x00_F000  phy_init                       (4 KB, radio calibration)
#   0x01_0000  factory                        (4 MB firmware image)
#   0x41_0000  storage                        (11.875 MB LittleFS, 3040 blocks)
#   0xFF_0000  coredump                       (64 KB panic crash dump)
#   0x100_0000 end of 16 MB flash
#
# Name,   Type, SubType,  Offset,    Size,      Flags
nvs,      data, nvs,      0x9000,    0x6000,
phy_init, data, phy,      0xf000,    0x1000,
factory,  app,  factory,  0x10000,   0x400000,
storage,  data, spiffs,   0x410000,  0xBE0000,
coredump, data, coredump, 0xff0000,  0x10000,
